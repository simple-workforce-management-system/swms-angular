import { AddJwtInterceptor } from './add-jwt-interceptor';

describe('AddJwtInterceptor', () => {
  it('should create an instance', () => {
    expect(new AddJwtInterceptor()).toBeTruthy();
  });
});
